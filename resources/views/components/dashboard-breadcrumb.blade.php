<div class="page-breadcrumb">
    <div class="row">
        <div class="col-12 d-flex no-block align-items-center">
            @if($url != '')
                <a href="{{ $url }}"><button class="btn btn-outline-info"> <i class="fas fa-arrow-left"></i> Quay lại</button></a>
            @endif
            <h3 class="ml-3 page-title">{{$title}}</h3>
        </div>
    </div>
</div>
